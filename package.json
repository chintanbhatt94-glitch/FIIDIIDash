/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // yahoo-finance2 ships Deno test files that webpack tries to bundle.
  // Mark it as an external package so Next.js loads it at runtime instead.
  experimental: {
    serverComponentsExternalPackages: ["yahoo-finance2"],
  },
};

module.exports = nextConfig;
